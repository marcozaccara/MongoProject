package mongo.enumerator;

/**
 * Enum to represent operations available in filter chain
 */
public enum OperatorEnum {
    EQUALS, NOT_EQUALS, LESS_THAN, LESS_THAN_EQUALS, GREATER_THAN, GREATER_THAN_EQUALS, IN, NOT_IN, MATCHES
}
